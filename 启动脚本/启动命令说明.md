# 系统启动命令说明

本文档说明如何启动新系统和旧系统。

---

## 📋 端口说明

- **旧系统（Streamlit看板）**：端口 8502
- **新系统后端（FastAPI）**：端口 8000
- **新系统前端（Vue）**：端口 5173

---

## 🔧 启动旧系统（Streamlit看板）

### 方法1：双击运行脚本（推荐）

直接双击运行：
```
启动脚本\启动旧系统.bat
```

### 方法2：使用CMD命令行

```cmd
REM 清理端口8502（如果被占用）
启动脚本\kill_port.bat 8502
REM 启动Streamlit看板
streamlit run dashboard.py --server.port 8502 --server.address 0.0.0.0
```

**访问地址：** http://localhost:8502

---

## 🚀 启动新系统（Vue + FastAPI）

### 方法1：双击运行脚本（推荐）

直接双击运行：
```
启动脚本\启动新系统.bat
```

脚本会自动：
- 清理被占用的端口（8000 和 5173）
- 启动后端API服务
- 启动前端服务
- 在独立窗口运行，便于查看日志

### 方法2：使用CMD命令行（需要打开两个终端窗口）

#### 终端1 - 启动后端API（固定端口8000）

```cmd
REM 清理端口8000（如果被占用）
启动脚本\kill_port.bat 8000
REM 启动后端API
cd backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

#### 终端2 - 启动前端（固定端口5173）

```cmd
REM 清理端口5173（如果被占用）
启动脚本\kill_port.bat 5173
REM 切换到frontend目录（重要：必须在这个目录下运行npm命令）
cd frontend
REM 启动前端
npm run dev -- --port 5173
```

**访问地址：**
- 后端API: http://localhost:8000
- API文档: http://localhost:8000/docs
- 前端页面: http://localhost:5173

---

## ⚠️ 注意事项

1. **目录位置**
   - 所有命令都需要在项目根目录（`D:\projects\line chart`）下运行
   - 前端命令必须先在 `frontend` 目录下运行

2. **端口占用处理**
   - `kill_port.bat` 会自动查找并终止占用端口的进程
   - 如果无法终止进程（需要管理员权限），请以管理员身份运行CMD
   - 手动查看端口占用：`netstat -ano | findstr ":端口号"`

3. **推荐使用批处理脚本**
   - 批处理脚本（`.bat`文件）会自动处理端口清理
   - 脚本会在新窗口运行服务，便于管理

4. **停止服务**
   - 如果使用批处理脚本启动，关闭对应的服务窗口即可停止
   - 如果使用命令行启动，在对应终端按 `Ctrl+C` 停止

---

## 🔍 故障排查

### 端口被占用

如果端口被占用，可以使用以下命令手动清理：

```cmd
REM 清理指定端口（例如：清理8000端口）
启动脚本\kill_port.bat 8000
```

### 查看端口占用情况

```cmd
REM 查看所有监听端口
netstat -ano | findstr "LISTENING"

REM 查看特定端口（例如：查看8000端口）
netstat -ano | findstr ":8000"
```

### 前端命令报错：找不到package.json

**错误信息：**
```
npm error enoent Could not read package.json
```

**解决方法：**
确保在 `frontend` 目录下运行 `npm` 命令：
```cmd
cd frontend
npm run dev -- --port 5173
```

### 后端命令报错：找不到模块

**错误信息：**
```
ModuleNotFoundError: No module named 'uvicorn'
```

**解决方法：**
安装后端依赖：
```cmd
cd backend
pip install -r requirements.txt
```

### 前端命令报错：找不到模块

**错误信息：**
```
Error: Cannot find module 'vite'
```

**解决方法：**
安装前端依赖：
```cmd
cd frontend
npm install
```

---

## 📝 快速参考

### 一键启动（推荐）

**旧系统：**
```
启动脚本\启动旧系统.bat
```

**新系统：**
```
启动脚本\启动新系统.bat
```

### 手动启动命令（CMD）

**旧系统：**
```cmd
启动脚本\kill_port.bat 8502
streamlit run dashboard.py --server.port 8502 --server.address 0.0.0.0
```

**新系统后端：**
```cmd
启动脚本\kill_port.bat 8000
cd backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**新系统前端：**
```cmd
启动脚本\kill_port.bat 5173
cd frontend
npm run dev -- --port 5173
```

---

## 🔗 相关文档

- 数据校验和收集命令：`校验当天\当天数据校验和收集命令.txt`
- 启动脚本说明：`启动脚本\README.md`

