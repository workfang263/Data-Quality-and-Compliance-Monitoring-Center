# FastAPI Backend - Python Dependencies

# 与项目根 requirements.txt 保持同一下限，避免 venv 漂移
loguru>=0.7.0

# FastAPI framework
fastapi>=0.104.1

# ASGI server (for running FastAPI)
uvicorn[standard]>=0.24.0

# MySQL database connection (same as old system)
pymysql>=1.1.0

# Data validation library (core dependency of FastAPI)
pydantic>=2.5.0

# Handle form data (multipart/form-data, for file uploads, etc.)
python-multipart>=0.0.6

# JWT token for authentication
PyJWT>=2.8.0

# HTTP client（店铺运营模块拉取店匠 OpenAPI）
requests>=2.31.0

