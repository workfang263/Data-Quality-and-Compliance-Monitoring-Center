# 项目完整性检查清单

根据 README2.md 的实施顺序，逐一检查每个步骤的完成情况。

---

## ✅ 步骤1：建表

### 1.1 数据库表检查
- [x] `fb_ad_account_spend_hourly` 表已创建
- [x] `owner_daily_summary` 表已创建
- [x] `store_owner_mapping` 表已创建
- [x] `ad_account_owner_mapping` 表已创建
- [x] `shoplazza_store_hourly` 表已添加 `owner` 字段

**状态：✅ 已完成**

---

## ✅ 步骤2：导入映射

### 2.1 映射数据检查
- [x] 店铺→负责人映射已导入（`store_owner_mapping`）
- [x] 广告账户→负责人映射已导入（`ad_account_owner_mapping`）

**验证命令：**
```sql
SELECT COUNT(*) FROM store_owner_mapping;
SELECT COUNT(*) FROM ad_account_owner_mapping;
```

**状态：✅ 已完成**

---

## ✅ 步骤3：花费采集脚本

### 3.1 脚本检查
- [x] `fb_spend_sync.py` 已创建
- [x] 采用覆盖模式（先清理再写入）
- [x] 支持日期过滤（过滤跨天数据）
- [x] 带重试机制
- [x] 支持代理设置

### 3.2 自动化集成检查
- [ ] ❌ **未完成**：`fb_spend_sync.py` 未集成到定时任务
- [ ] ❌ **未完成**：需要创建 Windows 定时任务，每5分钟执行一次

**需要创建：**
- `run_fb_spend_sync.bat` 批处理文件
- Windows 定时任务配置

**状态：⚠️ 部分完成（脚本已创建，但未自动化）**

---

## ✅ 步骤4：回填脚本

### 4.1 脚本检查
- [x] `fb_spend_backfill.py` 已创建
- [x] 采用覆盖模式（先清理再写入）
- [x] 支持日期过滤（过滤跨天数据）
- [x] 多线程并发
- [x] 带重试机制
- [x] 支持代理设置

### 4.2 历史数据回填检查
- [x] 最近30天数据已回填并验证
- [x] 30-90天数据已回填并验证

**状态：✅ 已完成**

---

## ⚠️ 步骤5：聚合任务

### 5.1 脚本检查
- [x] `aggregate_owner_daily.py` 已创建
- [x] 支持按日期范围聚合
- [x] 正确计算 ROAS（total_spend=0 时 roas=NULL）

### 5.2 历史数据聚合检查
- [ ] ❌ **未完成**：需要回填历史数据的 `owner` 字段（90天）
- [ ] ❌ **未完成**：需要重新聚合历史数据（90天）

**需要执行：**
```bash
# 1. 回填历史数据的 owner 字段
python backfill_owner_to_store_hourly.py --days 90

# 2. 重新聚合历史数据
python aggregate_owner_daily.py --days 90
```

### 5.3 自动化集成检查
- [ ] ❌ **未完成**：聚合任务未集成到自动化流程
- [ ] ❌ **未完成**：需要设置自动化聚合（方案A或方案B）

**方案A：独立定时任务**
- 创建 Windows 定时任务，每5分钟执行 `python aggregate_owner_daily.py`

**方案B：集成到同步脚本**
- 修改 `data_sync.py`，在数据写入完成后调用聚合脚本
- 修改 `fb_spend_sync.py`，在数据写入完成后调用聚合脚本

**状态：⚠️ 部分完成（脚本已创建，但历史数据未聚合，未自动化）**

---

## ❌ 步骤6：前端改造

### 6.1 主表："各负责人数据汇总"
- [ ] ❌ **未完成**：数据源改为 `owner_daily_summary`
- [ ] ❌ **未完成**：列显示：负责人、销售额、订单数、访客数、客单价、广告花费、ROAS
- [ ] ❌ **未完成**：支持日期范围选择
- [ ] ❌ **未完成**：支持单条编辑映射表
- [ ] ❌ **未完成**：移除左侧店铺多选，仅保留"总店铺"

### 6.2 折线图（点击负责人行弹出）
- [ ] ❌ **未完成**：数据源改为小时级原始表（按 owner 过滤）
- [ ] ❌ **未完成**：指标：销售额、订单数、访客数、客单价、广告花费、ROAS（可选开关）
- [ ] ❌ **未完成**：时间范围：默认当天，最多7天

### 6.3 现有店铺折线图与核心指标
- [ ] ❌ **未完成**：增加广告花费、ROAS
- [ ] ❌ **未完成**：核心汇总增加总广告花费、总 ROAS

**状态：❌ 未开始**

---

## ✅ 步骤7：验证

### 7.1 验证脚本检查
- [x] `verify_fb_spend_data.py` 已创建（验证广告花费数据准确性）
- [x] `verify_fb_spend_summary.py` 已创建（汇总查询，用于对比Facebook后台）

### 7.2 数据验证检查
- [x] 30天内数据已验证（全部 [OK]）
- [x] 30-90天数据已验证（全部 [OK]）
- [x] 聚合数据已验证（owner_daily_summary 数据正常）

**状态：✅ 已完成**

---

## 📋 待完成任务总结

### 高优先级（必须完成）

1. **回填历史数据的 owner 字段（90天）**
   ```bash
   python backfill_owner_to_store_hourly.py --days 90
   ```

2. **重新聚合历史数据（90天）**
   ```bash
   python aggregate_owner_daily.py --days 90
   ```

3. **设置 fb_spend_sync.py 的自动化**
   - 创建 `run_fb_spend_sync.bat`
   - 配置 Windows 定时任务，每5分钟执行一次

4. **设置聚合任务的自动化**
   - 方案A：创建独立定时任务
   - 方案B：集成到同步脚本

### 中优先级（下一步）

5. **前端改造**
   - 主表改为"各负责人数据汇总"
   - 点击负责人行弹出折线图
   - 现有折线图增加广告花费和ROAS

---

## 🎯 建议执行顺序

1. ✅ 先完成历史数据回填和聚合（步骤5.2）
2. ✅ 然后设置自动化（步骤3.2 和 步骤5.3）
3. ✅ 最后进行前端改造（步骤6）



