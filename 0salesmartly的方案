店匠 → SaleSmartly 订单同步中间件 — 方案一（轮询版）框架
一、目标与用途
要解决的问题：在 SaleSmartly 里打开客户聊天时，右侧能自动看到该客户的 7 个字段（订单号、订单金额、物流单号、付款时间、发货时间、姓名、邮箱），而不用再去店匠后台搜客户、查订单、复制单号。
本方案的做法：不接店匠 Webhook，用定时轮询店匠 OpenAPI 拉取订单数据，在程序内拼好 7 个字段后，调用 SaleSmartly 的「客户 + 订单」相关 API 推送。
用途：作为“店匠 ↔ SaleSmartly”的数据中转层，只做拉取、拼接、转发，不做业务系统改造。
二、整体架构
┌─────────────────┐      定时触发        ┌──────────────────────────┐│  计划任务        │  ────────────────►   │  同步程序（Python）        ││  (cron/计划程序) │  每 N 分钟一次       │  - 拉店匠订单              │└─────────────────┘                      │  - 拼 7 字段              │                                         │  - 推 SaleSmartly         │                                         └────────────┬───────────────┘                                                      │         ┌────────────────────────────────────────────┼────────────────────────────────────────────┐         │                                            │                                            │         ▼                                            ▼                                            ▼┌─────────────────┐                      ┌─────────────────────┐                      ┌─────────────────┐│  店匠 OpenAPI   │   GET /orders 等      │  本地状态（文件/     │   记录已同步订单号     │  SaleSmartly    ││  (订单/客户)    │  ◄────────────────   │  SQLite 等）         │  ◄────────────────   │  API            │└─────────────────┘                      └─────────────────────┘                      └─────────────────┘
无公网服务：不暴露 HTTP 端口，不需要 Webhook 接收地址，也不依赖内网穿透。
单向数据流：店匠 → 本程序 → SaleSmartly；配置与敏感信息放环境变量/配置文件。
三、设计思路
用“订单”为唯一主线
以店匠的订单号为唯一键，把「支付信息」和「发货信息」合并成一条记录，再映射到 SaleSmartly 的「客户 + 订单」。
增量同步，避免重复
只处理“未同步过”或“有更新”的订单：用本地状态（如已同步订单号集合 + 最后同步时间），每次只拉一段时间内的订单，只对新增/变更的订单调用 SaleSmartly。
配置与密钥外置
店匠 token、店铺域名、SaleSmartly API Key、轮询间隔等全部进配置文件或环境变量，便于部署和更换环境。
可观测
每次运行：记录“拉到的订单数、推送成功/失败、失败原因”；日志按天或按大小轮转，方便排查。
四、技术选型
项目	选型说明
语言	Python 3.10+
运行方式	单次执行脚本 + 系统计划任务（Windows 计划任务 / Linux cron）
HTTP 请求	requests 或 httpx
配置	.env + 简单 config.py，或仅 .env
持久化	优先轻量：JSON 文件或 SQLite，记录「已同步订单号」或「last_sync_time」即可
日志	logging 写文件 + 控制台，带时间、级别、简要内容
不强制用 Flask/FastAPI，因为本方案没有 HTTP 服务端，只做“定时跑一段脚本”。
五、7 个字段的来源与映射思路
字段	来源说明
订单号	店匠订单唯一标识，用作关联键
订单金额	店匠订单金额（如 total_price），对应 SaleSmartly 订单金额
物流单号	店匠订单的 fulfillment / 物流信息
付款时间	店匠 placed_at（或支付时间字段）
发货时间	店匠 fulfillment 创建/更新时间
姓名	店匠订单的 shipping/billing 姓名
邮箱	店匠订单的 email
逻辑：
每次轮询拉“最近一段时间”的订单（如 updated_at_min / updated_at_max 或按 placed_at）。
对每条订单：若本地状态里没有该订单号，或该订单“已发货”且本地记录未包含发货信息，则视为需同步。
用订单号 + 姓名 + 邮箱 在 SaleSmartly 侧对应到「客户」，再调用「导入客户订单」把订单号、金额、物流单号、付款时间、发货时间写进去。
（具体字段名以店匠、SaleSmartly 官方文档为准，这里只做逻辑映射。）
六、阶段步骤（实现顺序）
阶段 1：配置与项目骨架
新建独立项目目录（与 line chart 无关）。
建 .env.example 和 .env：店匠 shop_domain、access_token；SaleSmartly api_key、api_base_url（如 https://salesmartly-api.apifox.cn/）；轮询间隔 POLL_INTERVAL_MINUTES；可选店匠 Webhook 校验用 SHOPLAZZA_SECRET（本方案不用 Webhook，可预留）。
写 config.py：从环境变量读取上述配置。
写 logger：统一日志格式、落盘路径。
阶段 2：店匠侧 — 拉订单
封装店匠 API 客户端：根据店匠 OpenAPI 文档，实现「按时间范围拉订单列表」和「单笔订单详情（含 fulfillment）」。
入参：placed_at_min / placed_at_max 或 updated_at_min / updated_at_max（以文档为准）。
解析响应：取出订单号、金额、placed_at、email、姓名、fulfillment 状态、物流单号、发货时间等，输出为内部统一结构（如 dict 或 dataclass）。
阶段 3：本地状态 — 防重复
定义“已同步”存储：SQLite 表（推荐）或 JSON 文件。
字段建议：order_id（店匠订单号）、last_synced_at、可选 fulfillment_synced（是否已推送过发货信息）。
每次跑完同步后：把本次处理过的订单号写入；若 SaleSmartly 支持“更新订单”，则可根据订单号更新同一条订单的发货信息并标记 fulfillment_synced。
阶段 4：SaleSmartly 侧 — 推送
查 SaleSmartly 文档：客户 API（创建/更新客户，姓名、邮箱）、订单 API（导入客户订单：订单号、金额、物流单号、付款时间、发货时间等）。
实现两个封装：
ensure_contact(name, email)：按邮箱或姓名+邮箱查找/创建客户，返回可用于关联订单的 ID 或标识。
import_order(contact_id, order_data)：把 7 个字段里与订单相关的写入 SaleSmartly。
请求头中加入 SaleSmartly 要求的鉴权（如 API Key）。
错误处理：记录 HTTP 状态码、响应 body；失败时打日志并可选重试 1～2 次。
阶段 5：主流程串联
主流程伪代码：
加载配置与日志。
读本地状态，得到“上次同步截止时间”或“已同步订单号集合”。
调用店匠 API，拉取“自上次以来”的订单列表（及需要的详情）。
对每条订单：若未在已同步集合中，或发货信息有更新，则调用 SaleSmartly 的 ensure_contact + import_order，成功后更新本地状态。
写回本地状态（如更新 last_sync_time）。
打汇总日志：本次拉取数、推送成功数、失败数及原因。
阶段 6：部署与调度
在 Windows：用「任务计划程序」按间隔（如每 5 或 10 分钟）执行一次 python run_sync.py。
在 Linux：cron 如 */10 * * * * cd /path/to/project && python run_sync.py。
确保运行环境能访问店匠 API 和 SaleSmartly API（网络/代理若有需要一并配置）。
七、目录与模块划分建议
project_root/  .env  .env.example  config.py           # 配置加载  logger.py            # 日志配置  shoplazza_client.py  # 店匠 API 封装  salesmartly_client.py# SaleSmartly API 封装  state.py             # 本地状态（SQLite/JSON）  sync.py              # 主流程：拉取 → 拼接 → 推送 → 更新状态  run_sync.py          # 入口：被计划任务调用  requirements.txt  README.md
八、安全与健壮性
店匠：使用店匠给的 access_token，仅用于 OpenAPI 拉取，不暴露给公网。
SaleSmartly：API Key 只放在 .env，不提交到仓库；.env 加入 .gitignore。
幂等：同一订单多次推送时，依赖 SaleSmartly 的“按订单号更新”或“去重”能力；本地状态避免对同一订单重复推送。
日志：不打印完整 token/API Key；可打印订单号、时间范围、HTTP 状态码，便于排查。
九、与“方案二（Webhook）”的对比（便于你以后扩展）
维度	方案一（本方案）	方案二（Webhook）
实时性	延迟一个轮询周期	近实时
公网	不需要	需要可访问的 URL
状态存储	需要（已同步订单）	可选（按事件处理即可）
实现复杂度	略高（增量 + 合并）	较低（事件驱动）
当前文档是方案一的完整框架：设计、逻辑、技术、怎么做、用途、架构、阶段步骤和思路都涵盖在内。你按阶段 1～6 实现即可；若你愿意，我可以在 Agent 模式下按该框架在新建目录里帮你搭一版可运行的最小代码骨架（不含真实 API Key）。