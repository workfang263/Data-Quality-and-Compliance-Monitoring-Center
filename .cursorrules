1. 核心视觉原则 (Visual Principles)
“以后所有生成的前端代码，必须严格遵守以下审美约束：”
呼吸感 (Whitespace)： 严禁拥挤。容器内边距（Padding）默认至少为 p-6 或 p-8。组件间的间距（Gap）统一使用 gap-4 或 gap-6。
圆角控制 (Border Radius)： 放弃小圆角。容器统一使用 rounded-xl (12px) 或 rounded-2xl (16px)。按钮使用 rounded-lg。
色彩系统： 严禁使用纯黑 (#000)。背景色使用极浅灰 (bg-slate-50)，文本使用深灰色 (text-slate-800 或 text-slate-900)，以增加高级感。
阴影 (Shadows)： 严禁使用深色重阴影。统一使用物理光感阴影，例如 shadow-sm 配合细微边框 border border-slate-100，而非厚重的 shadow。
2. 针对 Element Plus 的魔改指令
“由于我们使用 Element Plus，请执行以下视觉覆盖：”
去除沉重边框： 给 el-card 设置 shadow="never"，并用 Tailwind 的 border-slate-100 替代。
输入框优化： 给 el-input 增加 hover:border-indigo-400 的平滑过渡，去掉默认的生硬蓝色边框。
表格美化： el-table 的表头必须使用浅色背景 (bg-slate-50/50)，字重设为 font-semibold。
3. 引用“高级感”参考系
“请在设计时参考 Linear, Vercel, 或 Apple 的界面风格：”
字体： 强制使用 font-sans 并优先调用 Inter 或 Geist 字体。
对比度： 辅助文字统一使用 text-slate-400，字号缩小到 text-xs 或 text-sm。