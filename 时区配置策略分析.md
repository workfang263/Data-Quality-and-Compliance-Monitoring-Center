# 时区配置策略分析

## 当前情况分析

### 需要配置为 UTC-8 的：
1. ✅ **负责人"小一"** - 已在步骤2中配置为 UTC-8（负责人级别）
2. ✅ **Shoplazza店铺 `luxelooks.myshoplaza.com`** - 属于"小一"，会自动使用负责人配置（UTC-8）
3. ✅ **Facebook广告账户 `act_1867789100501535`** - 需要单独配置为 UTC-8（账户级别）

### 需要保持 UTC+8 的：
1. ✅ **其他所有负责人** - 已在步骤2中默认配置为 UTC+8
2. ✅ **其他所有店铺** - 会自动使用各自负责人的配置（UTC+8）
3. ⚠️ **Facebook广告账户 `act_1899226041013050`** - 属于"小一"，但需要保持 UTC+8，所以需要单独配置为 UTC+8（覆盖负责人配置）

---

## 配置策略

### 方案A：最小化配置（推荐）

**已完成的配置：**
1. ✅ 所有负责人默认 UTC+8（步骤2）
2. ✅ "小一"负责人级别 UTC-8（步骤2）
3. ✅ 店铺 `luxelooks.myshoplaza.com` - **不需要单独配置**，会自动使用"小一"的配置（UTC-8）

**还需要配置的：**
1. ⚠️ Facebook账户 `act_1867789100501535` - 单独配置 UTC-8（账户级别）
2. ⚠️ Facebook账户 `act_1899226041013050` - 单独配置 UTC+8（账户级别，覆盖负责人配置）

**优点：**
- 配置最少
- 店铺自动继承负责人配置
- 只有特殊账户需要单独配置

---

### 方案B：显式配置（更清晰）

**已完成的配置：**
1. ✅ 所有负责人默认 UTC+8（步骤2）
2. ✅ "小一"负责人级别 UTC-8（步骤2）

**还需要配置的：**
1. ⚠️ 店铺 `luxelooks.myshoplaza.com` - 单独配置 UTC-8（店铺级别，显式配置）
2. ⚠️ Facebook账户 `act_1867789100501535` - 单独配置 UTC-8（账户级别）
3. ⚠️ Facebook账户 `act_1899226041013050` - 单独配置 UTC+8（账户级别）

**优点：**
- 配置更明确，一目了然
- 不依赖继承关系
- 如果未来"小一"的负责人配置改变，店铺配置不会受影响

---

## 时区配置优先级回顾

优先级从高到低：
1. **账户级别配置**（`ad_account_timezone_mapping`）
2. **店铺级别配置**（`store_timezone_mapping`）
3. **负责人级别配置**（`owner_timezone_mapping`）
4. **默认配置**（UTC+8）

---

## 推荐方案：方案A（最小化配置）

**理由：**
- 店铺 `luxelooks.myshoplaza.com` 属于"小一"，会自动使用"小一"的 UTC-8 配置
- 不需要重复配置
- 配置更简洁

**需要执行的配置：**
1. ✅ Facebook账户 `act_1867789100501535` - 配置 UTC-8（已在 `03_补充_配置账户时区.sql` 中）
2. ✅ Facebook账户 `act_1899226041013050` - 配置 UTC+8（已在 `03_补充_配置账户时区.sql` 中）

**店铺配置：**
- 不需要单独配置店铺
- 店铺会自动使用"小一"的负责人级别配置（UTC-8）

---

## 验证配置结果

执行完所有配置后，验证结果应该是：

### 负责人配置
```sql
SELECT owner, timezone, timezone_offset 
FROM owner_timezone_mapping 
WHERE owner = '小一';
```
**预期：** `小一 | America/Los_Angeles | -8.0`

### 店铺配置（查询时会使用负责人配置）
```sql
-- 店铺没有单独配置，会使用负责人配置
SELECT shop_domain, owner 
FROM store_owner_mapping 
WHERE owner = '小一';
-- 预期：luxelooks.myshoplaza.com | 小一
-- 实际使用时区：UTC-8（从负责人配置继承）
```

### 账户配置
```sql
SELECT ad_account_id, timezone, timezone_offset 
FROM ad_account_timezone_mapping 
WHERE ad_account_id IN ('act_1867789100501535', 'act_1899226041013050');
```
**预期：**
- `act_1867789100501535 | America/Los_Angeles | -8.0`
- `act_1899226041013050 | Asia/Shanghai | 8.0`

---

## 总结

**你的理解是正确的！**

✅ **其他都是默认 UTC+8**（已在步骤2中完成）
✅ **只需要单独配置这两个特殊账户**（已在 `03_补充_配置账户时区.sql` 中）

**店铺不需要单独配置**，因为：
- 店铺会自动使用负责人的配置
- "小一"的负责人配置已经是 UTC-8
- 所以店铺 `luxelooks.myshoplaza.com` 会自动使用 UTC-8

**当前配置状态：**
- ✅ 所有负责人默认 UTC+8
- ✅ "小一"负责人 UTC-8
- ✅ 店铺 `luxelooks.myshoplaza.com` - 自动使用"小一"配置（UTC-8）
- ✅ 账户 `act_1867789100501535` - 单独配置 UTC-8
- ✅ 账户 `act_1899226041013050` - 单独配置 UTC+8

**只需要执行 `03_补充_配置账户时区.sql` 即可！**




